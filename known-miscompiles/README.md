# Known ptxas miscompiles

Each subdirectory is a self-contained reproducer for one ptxas miscompilation
found by the differential fuzzer.

Contents per repro:

| File             | What it is                                                      |
| ---------------- | --------------------------------------------------------------- |
| `program.ptx`    | Generated kernel that miscompiles at `-O3`.                     |
| `input.bin`      | Per-thread input bytes (32 threads × 4 bytes = 128 bytes).      |
| `output_o0.bin`  | GPU output buffer after running the `-O0` cubin (correct).      |
| `output_o3.bin`  | GPU output buffer after running the `-O3` cubin (wrong).        |
| `seed.bin`       | Bytes that drove `ptx_fuzz_execgen::generate_from_bytes`.       |
| `summary.txt`    | One-liner from the fuzzer at save time.                         |
| `NOTES.md`       | Per-repro analysis (if present).                                |

To reproduce on the GPU:

```bash
./target/release/ptx-fuzz-diff-verify known-miscompiles/<name>
```

This will compile the saved PTX at both opt levels several times each, run
each multiple times, and report:

* Whether `-O0` and `-O3` agree (the miscompile, if any).
* Whether each opt level is internally deterministic.
* Whether ptxas itself is deterministic (recompile stability).

For an independent oracle (Rust hand-translation of the PTX semantics
compared against both opt levels), see per-repro `verify_*.rs` binaries in
`crates/ptx-fuzz-diff/src/bin/` where they exist.

## Environment

* Generator: `ptx-fuzz-execgen` defaults (see `crates/ptx-fuzz-execgen/src/lib.rs`).

Each repro records its own ptxas version, GPU arch, and driver in `NOTES.md`
(or a per-repro `summary.txt` for older ones). The repo has been run against
two boxes:

* Original A100 box: ptxas 13.2 V13.2.78 (Build 37668154), driver 580.126.20,
  target `sm_80`.
* Current B300 box: ptxas 13.0 V13.0.88 (Build 36424714), driver 580.126.20,
  target `sm_103`.

## Index

### m001-seed-050f

First verified miscompile. Generated by seed `0x50f` (1295), 4096 input bytes.
`-O3` produces wrong output for tids 0..19 (those that take the
`block_0 → block_5` direct path). Differences: `output[2]` off by 16,
`output[3]` off by 32. Tids 20..31 (going via `block_0 → block_3 → block_4`)
are unaffected.

Independent verification: `cargo run -p ptx-fuzz-diff --release --bin verify_050f`
runs a hand-translated Rust implementation of `program.ptx` and confirms it
matches `-O0` on all 32 threads and disagrees with `-O3` on the same 20 tids
the differential reported.

Not yet reduced — still ~60 lines of body PTX, 7 reachable blocks (0, 2, 3,
4, 5, 6; block_1 is statically unreachable), 2 loops with bounded counters.

**Reproduces on sm_103 too.** Originally found on the A100 box (ptxas 13.2,
sm_80). On 2026-05-13, verified the same saved repro still misbehaves on the
B300 box (ptxas 13.0 V13.0.88, sm_103) — same 20 affected tids, same byte
deltas — both when ptxas cross-compiles the `.target sm_80` source for
`-arch=sm_103` and when the source is rewritten to `.target sm_103` natively
(with `.version 8.8`). So it isn't an sm_80-specific lowering bug; the
optimizer pass that miscomputes the live-out values at the loop join is
shared across both target backends.
